paths:
  /api/users/{uid}:
    delete:
      summary: Eliminar usuario
      description: Delete a user
      tags:
        - Users
      parameters:
        - in: path
          name: uid
          required: true
          description: User Id
          schema:
            type: string
      responses:
        200:
          description: Delete user